# Link Bio v3 hardening

Validated profile asset, v3 localization/runtime cleanup, privacy page, disabled analytics/IP-geolocation runtime, legacy-route cleanup, mobile performance fixes, stale asset cleanup, and CI validation.
